{% extends 'base.html.twig' %}

{% block title %}Edit Contact{% endblock %}

{% block body %}
   
        <div class="p-5 form_contact">
            <div class="d-flex flex-row justify-content-between align-items-center ">
                <h1 class="gothicBold">Edit Contact</h1>
                <a class="btn btn-success rounded-pill px-4 mx-2" href="{{ path('app_contact_index') }}">Retour à la liste</a>
            </div>
            {{ include('contact/_form.html.twig') }}
        </div>
{% endblock %}
